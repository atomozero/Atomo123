# Build artifacts (legacy BeOS/Haiku Makefiles)
Obj*/
obj.*/
dep.*/
lib.*/
*.o
*.a
*.rsrc

# Build artifacts (engine/ Makefile)
engine/obj/
engine/libengine.a
engine/tests/smoke_test
engine/tests/named_functions_test

# Build artifacts (translators/*/ Makefiles)
translators/*/CsvTranslator
translators/*/XlsTranslator
translators/*/XlsxTranslator
translators/*/OdsTranslator
translators/*/tests/test_*_translator

# Build artifacts (ui/ Makefile)
ui/Atomo123
ui/src/*.o
ui/Atomo123.rsrc
# Copie del binario rinominate con l'hash del commit (invece di
# cancellarle con "make clean"), vedi CLAUDE.md/istruzioni di sessione
ui/Atomo123-*
ui/examples/generate_demo
ui/examples/atomo123_demo.ascd

# Compiled binaries produced by the legacy build (not source)
legacy/opensumit/bsl/bsl
legacy/opensumit/rez/rez
legacy/opensumit/sum-it/OpenSum-It

# CVS metadata carried over from the original historical repo
**/CVS/

# Editor/OS cruft
.DS_Store
*.swp
.claude/
